format = """
$directory$git_branch$git_status$fill$cmd_duration
$character"""

[character]
success_symbol = "❯"
error_symbol = "❯"

[directory]
truncation_length = 3

[git_status]
ahead = "⇡${count}"
behind = "⇣${count}"
diverged = "⇕⇡${ahead_count}⇣${behind_count}"
